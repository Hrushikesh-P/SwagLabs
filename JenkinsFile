pipeline {
   agent { docker { image 'my-playwright-image:latest' } }
   stages {
      stage('e2e-tests') {
         steps {
            sh 'npm ci'
            sh 'npx playwright test'
         }
      }
   }
}